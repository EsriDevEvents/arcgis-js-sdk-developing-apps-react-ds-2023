import { Box, Typography, styled } from "@mui/material";

const HomeContainer = styled(Box)({
  padding: "20px",
});

const TypographyParagraph = styled(Typography)({
  marginTop: "10px",
});

export { HomeContainer, TypographyParagraph };
